apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: tax-access-control
  namespace: default
spec:
  selector:
    matchLabels:
      app: tax
  action: ALLOW
  rules:
  - from:
    - source:
        principals: ["cluster.local/ns/default/sa/order-service-account"]